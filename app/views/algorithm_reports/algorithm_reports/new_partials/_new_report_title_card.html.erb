<!-- LIGHT STORYBOOK-STYLE QUEST INITIALIZATION PANEL -->
<div class="max-w-5xl mx-auto my-10 p-8 bg-gradient-to-b from-white to-slate-50/50 border-2 border-slate-100 rounded-[38px] shadow-md relative overflow-hidden">

  <!-- Subtle Ambient Background Glow Elements -->
  <div class="absolute -top-24 -left-24 w-72 h-72 bg-amber-100/40 rounded-full blur-3xl animate-pulse"></div>
  <div class="absolute -bottom-24 -right-24 w-72 h-72 bg-emerald-100/30 rounded-full blur-3xl animate-pulse delay-75"></div>

  <!-- Quest Header -->
  <div class="flex items-center justify-between mb-10 pb-5 border-b-2 border-slate-100 relative z-10">
    <div class="flex items-center space-x-4">
      <div class="w-9 h-9 rounded-2xl bg-amber-100/70 flex items-center justify-center text-amber-600 text-lg animate-bounce">
        📜
      </div>
      <div>
        <h3 class="text-sm font-black uppercase tracking-wider text-slate-700">New Report Initialization</h3>
        <p class="text-[12px] text-slate-400 font-medium">Generate an execution instance based on a master algorithm</p>
      </div>
    </div>
    <span class="text-[12px] font-bold text-amber-600 bg-amber-50 px-3.5 py-1.5 rounded-full border border-amber-100/60 shadow-sm uppercase animate-pulse">
      ✨ Level 1 Campaign
    </span>
  </div>

  <!-- HERO ANIMATION & WELCOME TEXT -->
  <div class="flex flex-col items-center justify-center text-center my-6 relative z-10">

    <!-- LIGHT STORYBOOK ANIMATED GRAPHIC LAYER -->
    <div class="relative w-32 h-32 mb-6 flex items-center justify-center">
      <!-- Outer Rotating Segmented Border -->
      <div class="absolute inset-0 border-4 border-dashed border-amber-300/60 rounded-full animate-[spin_20s_linear_infinite]"></div>

      <!-- Pulsing Inner Target Wave -->
      <div class="absolute w-24 h-24 bg-gradient-to-tr from-amber-100 to-amber-200/50 rounded-[26px] opacity-60 blur-sm animate-ping"></div>

      <!-- Main Bouncing Core Diamond Grid Anchor -->
      <div class="absolute w-20 h-20 bg-amber-50 border-[3px] border-amber-400 flex items-center justify-center text-amber-700 font-black shadow-md shadow-amber-200/60 ring-[5px] ring-amber-100/50 rounded-[22px] transform rotate-45 animate-[bounce_3s_ease-in-out_infinite] hover:scale-105 transition-all duration-200">
        <!-- Re-rotate inner indicator so emblem remains upright -->
        <span class="transform -rotate-45 text-3xl select-none filter drop-shadow-sm">✨</span>
      </div>

      <!-- Satellite Spark Particles -->
      <div class="absolute w-3 h-3 bg-emerald-400 rounded-full top-2 right-2 animate-ping"></div>
      <div class="absolute w-2.5 h-2.5 bg-sky-400 rounded-full bottom-3 left-1 animate-pulse"></div>
    </div>

    <!-- MAIN QUEST TITLES -->
    <h1 class="text-2xl md:text-3xl font-black text-slate-800 tracking-wide mb-3">
      Initialize Global Blueprint Report
    </h1>

    <p class="max-w-xl text-[13px] font-medium text-slate-500 leading-relaxed">
      You are about to launch a sequential execution path. Lay down your foundation parameters below to map, construct, and transform raw nodes into an automated workflow engine.
    </p>

    <!-- HORIZONTAL STORYBOOK STEPS PREVIEW PREVIEW CONTAINER -->
    <div class="flex items-center space-x-3 mt-8 bg-slate-50 px-5 py-2.5 rounded-2xl border border-slate-100 shadow-inner">
      <span class="text-[11px] font-bold text-slate-400 tracking-wider uppercase">Campaign Progress:</span>
      <span class="text-amber-700 text-xs font-black bg-amber-50 px-2 py-0.5 rounded-md border border-amber-200/40">01. Setup Report</span>
      <span class="text-slate-300 text-xs font-bold">➔</span>
      <span class="text-slate-400 text-xs font-medium">02. Steps</span>
      <span class="text-slate-300 text-xs font-bold">➔</span>
      <span class="text-slate-400 text-xs font-medium">03. Finish</span>
    </div>

  </div>

</div>
