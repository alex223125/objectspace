<% path = @folder.new_record? ? folders_path : folder_path %>
<% method = @folder.new_record? ? :post : :patch %>
<%= simple_form_for(@folder, url: path, method: method) do |f| %>

  <% if @folder.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(@folder.errors.count, "error") %> prohibited this Class from being saved:</h2>

      <ul>
        <% @folder.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-inputs">
    <div class="col-span-6 sm:col-span-3">
      <%= f.label "Title", class: "block text-sm font-medium text-gray-700" %>
      <%= f.hint 'Describe briefly what items you plan to store in folder',
                 class: "mt-2 text-sm text-gray-500" %>
      <%= f.input_field :title,
                        required: true,
                        class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
                        placeholder: "Ex: Methodologies how o create Ruby on Rails Blog applications" %>
    </div>
    <br>

    <div class="col-span-6 sm:col-span-3" data-controller="folder_description_tinymce">
      <%= f.label "Description", class: "block text-sm font-medium text-gray-700" %>
      <%= f.hint 'Describe in more detailed way responsibility of this folder. Thats the description which will be shown on the top of the source page',
                 class: "mt-2 text-sm text-gray-500" %>
      <%= f.input_field :description,
                        required: false,
                        class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
                        placeholder: "Ex: Different methodologies about ruby, ruby on rails, postgresql",
                        data: { target: 'folder_description_tinymce.field' }%>
    </div>
    <hr class="w-48 h-1 mx-auto my-4 bg-gray-100 border-0 rounded md:my-10 dark:bg-gray-700">

    <% if @target.present? && @target_type == 'folder' %>
      <%= hidden_field_tag 'target_folder_id', @target.id  %>
    <% elsif @target.present? && @target_type == 'repository' %>
      <%= hidden_field_tag 'target_repository_id', @target.id  %>
    <% end %>

    <%= render "folders/partials/subfolders/nested_form", :form => f %>


    <div class="bg-gray-50 px-4 py-3 sm:px-6">
      <%#= link_to "Show this folder", target_folder_path(username: @folder.root.repository.user.username, id: folder.slug)  %>
      <div class="text-end">
        <% if @folder.new_record? %>
          <% if @target.is_a?(Repository) %>
            <%= link_to "Back to Repository", target_repository_path(username: @target.user.username, id: @target.slug), class: "py-2.5 px-5 mr-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 hover:text-blue-700 focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700" %>
          <% elsif @target.is_a?(Folder) %>
            <%= link_to "Back to Folder", target_folder_path(username: @target.root.repository.user.username, id: @folder.slug), class: "py-2.5 px-5 mr-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 hover:text-blue-700 focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700" %>
          <% end %>
          <%= f.button :submit, "Create Folder", class: "inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
        <% else %>
          <%= link_to "Back to Folder", target_folder_path(username: @folder.root.repository.user.username, id: @folder.slug), class: "py-2.5 px-5 mr-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 hover:text-blue-700 focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700" %>
          <%= f.button :submit, "Update Folder", class: "inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
        <% end %>

      </div>
    </div>

  </div>

<% end %>
